# Local Docker scripts

- `start.sh` and `stop.sh` are for macOS and Linux shells.
- `start.ps1` and `stop.ps1` are for Windows PowerShell.

Keep these scripts thin wrappers around Docker Compose. They must start and stop
the same application without host-language setup steps.
